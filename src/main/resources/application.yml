spring:
  application:
    name: kieran-ai-app-builder-backend
server:
  port: 8123
  servlet:
    context-path: /api
# springdoc-openapi
springdoc:
  group-configs:
    - group: 'default'
      packages-to-scan: pers.kieran.study.kieranaiappbuilder.controller
# knife4j
knife4j:
  enable: true
  setting:
    language: zh_cn
